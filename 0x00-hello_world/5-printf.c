#include <stdio.h>
/**
 * main - Entry point.
 * main: Prints the specified string using the puts function.
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art,");
return (0);
}
