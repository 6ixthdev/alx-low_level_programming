#include "main.h"
/**
 *add - adds two integers
 *add: adds two integers
 *@a: one of the arguments
 *@b: another argument
 *Return: integer value
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
